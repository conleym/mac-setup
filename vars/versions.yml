# Explicit software versions to install.
# Remember to enclose versions ending in .0 in quotes. Probably best to just quote them all, really.
---
gpgtools_version: "2022.2"
iterm2_version: "3.4.18"
macports_version: "2.8.1"
node_version: "18.15.0"
node_npm_version: "9.6.4"
picard_version: "2.8.5"
r_version: "4.2.2"
skim_version: "1.6.13"
terraform_version: "1.3.6"
vagrant_version: "2.3.4"
visualvm_version: "2.1.5"
vlc_version: "3.0.18"
xact_version: "2.51"
xquartz_version: "2.8.3"

# Versions as reported by VBoxManage --version are of the form {{ virtualbox_version }}-r{{ virtualbox_revision }}.
# Both numbers appear in the dmg URL. These two must be kept in sync with each release.
virtualbox_version: "7.0.4"
virtualbox_revision: "154605"

# Ngrok stores binaries using some sort of hash.
ngrok_hash: "bNyj1mQVY4c"
